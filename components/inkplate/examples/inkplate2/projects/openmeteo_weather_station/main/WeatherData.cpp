/**
 * @file        WeatherData.cpp
 * @author      Fran Fodor for Soldered
 * @brief       Plain data container for the Open-Meteo weather dashboard.
 *
 * @details     WeatherData is a pure data holder (see WeatherData.h); all of
 *              its members are given default values in the class definition,
 *              so there is nothing to define here. This translation unit
 *              only exists to mirror the original Arduino project's
 *              src/WeatherData.cpp file 1:1.
 */

#include "WeatherData.h"
