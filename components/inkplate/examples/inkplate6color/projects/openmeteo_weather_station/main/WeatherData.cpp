/**
 * @file        WeatherData.cpp
 * @brief       WeatherData has no behavior of its own (plain data holder);
 *              kept as a separate translation unit only to mirror the
 *              original Arduino example's src/ file split.
 */

#include "WeatherData.h"
